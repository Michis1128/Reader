<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/bottomControls"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="14dp"
    android:paddingTop="5dp"
    android:paddingEnd="14dp"
    android:paddingBottom="7dp">

    <LinearLayout
        android:id="@+id/pageJumpActions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        android:visibility="gone">

        <Button
            android:id="@+id/jumpBackButton"
            style="@style/Widget.MichisReader.Button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:minWidth="0dp"
            android:text="Regresar a página 1"
            android:textAllCaps="false"
            android:textSize="12sp"
            android:visibility="gone" />

        <Button
            android:id="@+id/clearJumpHistoryButton"
            style="@style/Widget.MichisReader.Button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/ui_content_spacing"
            android:layout_weight="1"
            android:minWidth="0dp"
            android:text="Limpiar historial"
            android:textAllCaps="false"
            android:textSize="12sp"
            android:visibility="gone" />

        <Button
            android:id="@+id/jumpForwardButton"
            style="@style/Widget.MichisReader.Button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/ui_content_spacing"
            android:layout_weight="1"
            android:minWidth="0dp"
            android:text="Avanzar a página 1"
            android:textAllCaps="false"
            android:textSize="12sp"
            android:visibility="invisible" />
    </LinearLayout>

    <TextView
        android:id="@+id/progressLabel"
        android:layout_width="match_parent"
        android:layout_height="28dp"
        android:gravity="center"
        android:text="Abriendo EPUB…" />

    <SeekBar
        android:id="@+id/progressSlider"
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:max="1" />
</LinearLayout>
